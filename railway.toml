[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyMaxRetries = 10
restartPolicyType = "ON_FAILURE"
startCommand = "python debug_server.py" 